{
    "name": "fresh-bloom/payment-system",
    "description": "Fresh Bloom flower shop with Stripe payment integration using PHP",
    "type": "project",
    "require": {
        "php": ">=7.4",
        "stripe/stripe-php": "^10.0"
    },
    "autoload": {
        "psr-4": {
            "FreshBloom\\": "src/"
        }
    },
    "scripts": {
        "post-install-cmd": [
            "php -r \"copy('.env.example', '.env');\""
        ]
    }
}